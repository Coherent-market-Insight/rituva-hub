# Database Configuration - SQLite + Turso Setup
# For local development, SQLite is used (file-based, zero setup)
# For production on Vercel, switch to Turso provider

# ==================================================
# LOCAL DEVELOPMENT (SQLite)
# ==================================================
# Database file path - auto-created on first run
DATABASE_URL="file:./prisma/dev.db"

# ==================================================
# PRODUCTION (Turso - Switch to this for Vercel)
# ==================================================
# DATABASE_URL="libsql://your-db-name-youruser.turso.io"
# DATABASE_AUTH_TOKEN="your-turso-auth-token"

# ==================================================
# Authentication
# ==================================================
# JWT Secret for token signing (min 32 characters)
JWT_SECRET="your-secret-key-min-32-chars-change-this"

# NextAuth Secret (min 32 characters)
NEXTAUTH_SECRET="your-nextauth-secret-min-32-chars-change-this"

# ==================================================
# Site Configuration
# ==================================================
# Your application URL
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Environment
NODE_ENV="development"

# ==================================================
# Session Configuration
# ==================================================
# Session max age in seconds (30 days)
SESSION_MAX_AGE=2592000

# ==================================================
# Optional: Email Services (for production)
# ==================================================
# SENDGRID_API_KEY=your_sendgrid_key
# RESEND_API_KEY=your_resend_key

# ==================================================
# DEPLOYMENT NOTES
# ==================================================
# 
# 1. LOCAL DEVELOPMENT:
#    - Use SQLite (DATABASE_URL="file:./prisma/dev.db")
#    - No setup needed
#    - Prisma schema provider: sqlite
#
# 2. PRODUCTION ON VERCEL:
#    - Create Turso account: https://turso.tech (free)
#    - Create database: turso db create project-hub
#    - Get connection string: turso db show --url project-hub
#    - Get auth token: turso db tokens create project-hub
#    - Set env vars in Vercel dashboard
#    - Switch Prisma provider to "turso"
#
# 3. GENERATING SECRETS:
#    PowerShell:
#      [Convert]::ToBase64String((1..32 | ForEach-Object {[byte](Get-Random -Max 256)}))
#    
#    Node.js:
#      node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
