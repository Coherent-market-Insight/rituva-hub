# Project Hub - Visual Guide & Diagrams

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                   USER BROWSER                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Next.js Client Components                            │  │
│  │  - Pages (Login, Dashboard, etc.)                     │  │
│  │  - React Components with TypeScript                   │  │
│  │  - Tailwind CSS + Radix UI                            │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTPS/JWT Token
                           ↓
┌──────────────────────────────────────────────────────────────┐
│            NEXT.JS APPLICATION SERVER                        │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  API Routes                                            │  │
│  │  ├── /api/auth/* (Signup, Login, Logout)             │  │
│  │  ├── /api/projects/* (CRUD operations)               │  │
│  │  └── /api/tasks/* (Task management)                  │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Middleware & Utilities                              │  │
│  │  ├── lib/auth.ts (JWT verification)                  │  │
│  │  ├── lib/authorization.ts (Permission checks)        │  │
│  │  └── lib/db.ts (Prisma client)                       │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────┬───────────────────────────────────┘
                           │ SQL/Prisma ORM
                           ↓
┌──────────────────────────────────────────────────────────────┐
│            POSTGRESQL DATABASE                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Tables:                                               │  │
│  │  ├── users, workspaces, projects, boards, tasks       │  │
│  │  ├── members (with roles), comments                   │  │
│  │  └── activity_logs, audit_logs, notifications        │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## 🔐 Authorization Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│                    SYSTEM LEVEL                             │
│                                                             │
│  super_admin ─────────────────────────> Full Access        │
│  user ────────────────────────────────> Limited Access     │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                   WORKSPACE LEVEL                           │
│                                                             │
│  owner  ──────> Can delete workspace, manage all settings   │
│  admin  ──────> Can manage members and workspace settings   │
│  member ──────> Can access projects within workspace        │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                    PROJECT LEVEL                            │
│                                                             │
│  owner   ──────> Can delete project, full control           │
│  lead    ──────> Can manage team members & settings         │
│  manager ──────> Can manage tasks and assignments           │
│  member  ──────> Can work on assigned tasks                 │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Authentication Flow

```
User                           API Server                    Database
  │                              │                              │
  │ 1. Enter credentials        │                              │
  ├─────────────────────────────>│                              │
  │                              │ 2. Find user by email       │
  │                              ├─────────────────────────────>│
  │                              │<─────────────────────────────┤
  │                              │ 3. Hash input password       │
  │                              │    Compare with stored hash  │
  │                              │ 4. Generate JWT token       │
  │                              │ 5. Set HTTP-only cookie    │
  │ 6. Login successful          │                              │
  │<─────────────────────────────┤                              │
  │ (Token in cookie)            │                              │
  │                              │                              │
  │ 7. Request with JWT         │                              │
  ├─────────────────────────────>│                              │
  │                              │ 8. Verify JWT signature    │
  │                              │ 9. Extract user ID         │
  │                              │ 10. Verify permissions     │
  │                              ├─────────────────────────────>│
  │                              │<─────────────────────────────┤
  │ 11. Access granted          │                              │
  │<─────────────────────────────┤                              │
  │                              │                              │
```

## 🗄️ Database Relationships

```
User (1) ──────────── (Many) WorkspaceMember
                             │
                             ├──> Workspace (Many)
                             │

User (1) ──────────── (Many) ProjectMember
                             │
                             ├──> Project (Many)
                             │

Project (1) ─────────── (Many) Board (1) ─────────── (Many) Task
                                                        │
                                                        ├──> TaskComment
                                                        ├──> ActivityLog
                                                        └──> User (assigned)

Project (1) ─────────── (Many) ActivityLog
                                │
                                └──> User (performed by)

Workspace (1) ────────── (Many) ActivityLog
```

## 📋 Data Models at a Glance

```
┌─────────────────────┐
│       User          │
├─────────────────────┤
│ • id                │
│ • email (unique)    │
│ • password_hash     │
│ • full_name         │
│ • avatar_url        │
│ • role              │
│ • is_email_verified │
└─────────────────────┘
         │ Many-to-Many (with join tables)
         ├─────> WorkspaceMember
         ├─────> ProjectMember
         ├─────> Task (assigned/created)
         └─────> ActivityLog

┌──────────────────────┐
│     Workspace        │
├──────────────────────┤
│ • id                 │
│ • name               │
│ • slug (unique)      │
│ • description        │
│ • avatar_url         │
└──────────────────────┘
         │
         ├─────> WorkspaceMember (Many)
         ├─────> Project (Many)
         └─────> ActivityLog (Many)

┌──────────────────────┐
│      Project         │
├──────────────────────┤
│ • id                 │
│ • name               │
│ • description        │
│ • slug (unique)      │
│ • owner_id           │
│ • workspace_id       │
│ • color              │
└──────────────────────┘
         │
         ├─────> ProjectMember (Many)
         ├─────> Board (Many)
         └─────> ActivityLog (Many)

┌──────────────────────┐
│       Board          │
├──────────────────────┤
│ • id                 │
│ • name               │
│ • project_id         │
│ • position           │
└──────────────────────┘
         │
         └─────> Task (Many)

┌──────────────────────┐
│        Task          │
├──────────────────────┤
│ • id                 │
│ • title              │
│ • description        │
│ • board_id           │
│ • position           │
│ • priority           │
│ • status             │
│ • due_date           │
│ • assigned_to        │
│ • created_by         │
└──────────────────────┘
         │
         ├─────> TaskComment (Many)
         └─────> ActivityLog (Many)
```

## 🚀 Deployment Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    OPTION 1: VERCEL + RAILWAY              │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────┐         ┌──────────────────┐       │
│  │  Vercel          │         │   Railway        │       │
│  │  (Next.js App)   │         │  (PostgreSQL)    │       │
│  │                  │◄───────>│                  │       │
│  │  - Auto deploy   │  HTTPS  │  - Auto backups  │       │
│  │  - Edge caching  │         │  - Scaling ready │       │
│  │  - Free tier     │         │  - Free $5 tier  │       │
│  └──────────────────┘         └──────────────────┘       │
│         │                              │                 │
│         └──────────┬───────────────────┘                 │
│                    ↓                                     │
│            Your application live!                       │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                    OPTION 2: RENDER (ALL-IN-ONE)          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────────────────────────────────┐        │
│  │  Render                                      │        │
│  │  ┌─────────────────┐ ┌─────────────────┐   │        │
│  │  │ Next.js Web     │ │ PostgreSQL      │   │        │
│  │  │ Service (free)  │ │ Database (free) │   │        │
│  │  └────────┬────────┘ └────────┬────────┘   │        │
│  │           └────────┬─────────┘              │        │
│  │                    ↓                        │        │
│  │         Both hosted on Render!             │        │
│  └──────────────────────────────────────────────┘        │
└────────────────────────────────────────────────────────────┘
```

## 🔄 API Request/Response Cycle

```
Client Browser                Next.js API               Database
      │                              │                     │
      │ POST /api/projects          │                     │
      │ ├─ auth token in cookie     │                     │
      │ ├─ request body: JSON       │                     │
      ├─────────────────────────────>                     │
      │                              │                     │
      │                    1. Extract JWT from cookie     │
      │                    2. Verify signature            │
      │                    3. Get user ID                 │
      │                              │                     │
      │                    4. Check authorization         │
      │                              ├────────────────────>
      │                              │ SELECT * FROM users
      │                              │ WHERE id = ?        │
      │                              │<────────────────────┤
      │                              │ user_found (owner)  │
      │                              │                     │
      │                    5. Create project in DB        │
      │                              ├────────────────────>
      │                              │ INSERT INTO projects
      │                              │<────────────────────┤
      │                              │ project_created    │
      │                              │                     │
      │                    6. Log activity               │
      │                              ├────────────────────>
      │                              │ INSERT INTO logs    │
      │                              │<────────────────────┤
      │                              │ logged              │
      │                              │                     │
      │ Response: 201 Created        │                     │
      │ ├─ success: true             │                     │
      │ ├─ data: { project }         │                     │
      │<─────────────────────────────┤                     │
      │                              │                     │
      │ UI Updates with new project  │                     │
      │                              │                     │
```

## 🎯 Key Workflows

### Project Creation Workflow
```
1. User clicks "New Project" ──────────────┐
                                           ↓
2. Form validation (client-side) ◄─── Tailwind form
                                           ↓
3. POST /api/projects ◄────────────── JWT token in cookie
                                           ↓
4. Server: Verify JWT ◄──────────────── Extract from cookie
                                           ↓
5. Server: Check permission ◄────── Can user create?
                                           ↓
6. Server: Create project ◄────── INSERT into database
                                           ↓
7. Server: Log activity ◄────────── ActivityLog table
                                           ↓
8. Return project data ◄──────────── 201 Created
                                           ↓
9. UI adds project to list ◄─── Dashboard re-renders
                                           ↓
10. Success notification ◄────────── Sonner toast
```

### Task Assignment Workflow
```
1. User selects task ──────────────┐
                                   ↓
2. Click "Assign to" ◄────── Dropdown menu
                                   ↓
3. Select team member ◄────── List of project members
                                   ↓
4. PUT /api/tasks/[id] ◄────── Member ID in request
                                   ↓
5. Server: Verify JWT ◄────────── Authentication check
                                   ↓
6. Server: Check permission ◄─── Can user edit project?
                                   ↓
7. Server: Update task ◄────── UPDATE in database
                                   ↓
8. Server: Log activity ◄─────── ActivityLog entry
                                   ↓
9. Notify assignee ◄───────────── Notification created
                                   ↓
10. Return updated task ◄─────── Task object with assignee
                                   ↓
11. UI updates task card ◄────── Show assigned user
```

## 📊 Status Code Meanings

```
┌─────┬─────────────────────────────────────────────────┐
│ 200 │ OK - Request successful                         │
├─────┼─────────────────────────────────────────────────┤
│ 201 │ Created - Resource created successfully         │
├─────┼─────────────────────────────────────────────────┤
│ 400 │ Bad Request - Invalid request format            │
├─────┼─────────────────────────────────────────────────┤
│ 401 │ Unauthorized - Not authenticated (no JWT)       │
├─────┼─────────────────────────────────────────────────┤
│ 403 │ Forbidden - Authenticated but no permission     │
├─────┼─────────────────────────────────────────────────┤
│ 404 │ Not Found - Resource doesn't exist              │
├─────┼─────────────────────────────────────────────────┤
│ 500 │ Server Error - Internal error                   │
└─────┴─────────────────────────────────────────────────┘
```

## 🔄 Feature Interactions

```
Dashboard
    ├── Projects List
    │   ├── Create Project ──────> Project Created
    │   └── Click Project ──────> Project Details
    │
    ├── Project Details
    │   ├── Team Management
    │   │   ├── Invite Member ──> Member Added
    │   │   ├── Update Role ────> Role Changed
    │   │   └── Remove Member ─> Member Removed
    │   │
    │   └── Task Management
    │       ├── Create Task ────> Task Created
    │       ├── View Tasks ─────> Kanban Boards
    │       ├── Assign Task ────> Task Assigned
    │       ├── Update Status ──> Status Changed
    │       └── Add Comment ────> Comment Added
    │
    ├── Activity Feed
    │   └── View all actions
    │
    └── Settings
        └── Update profile
```

---

## 📈 Performance Targets

```
Frontend Performance:
├── Page Load Time ──────────────> < 2 seconds
├── API Response Time ───────────> < 500ms
├── UI Responsiveness ───────────> 60 FPS
└── Bundle Size ─────────────────> < 200KB

Backend Performance:
├── Database Query Time ─────────> < 100ms
├── Authentication Check ────────> < 50ms
├── Authorization Check ─────────> < 50ms
└── Concurrent Requests ─────────> 1000+

Scalability:
├── Support Users ────────────────> 10,000+
├── Support Projects ────────────> 100,000+
├── Support Tasks ────────────────> 1,000,000+
└── Database Size ───────────────> Multi-GB
```

---

These diagrams illustrate how Project Hub's architecture, authorization, data flow, and deployment work together to provide a robust, scalable project management solution! 🚀

